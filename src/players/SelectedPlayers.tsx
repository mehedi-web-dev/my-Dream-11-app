export interface SelectedPlayersProps {
  prop: string
}

const SelectedPlayers = ({ prop }: SelectedPlayersProps) => {
  
  return (
    <div>
      <h3>Not selected Players</h3>
    </div>
  )
}

export default SelectedPlayers;